{
  "name": "cornerkicks-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply conerkicks-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply conerkicks-db --remote"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "drizzle-orm": "^0.39.0",
    "jose": "^5.10.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@cloudflare/workers-types": "^4.20250204.0",
    "wrangler": "^3.105.0",
    "drizzle-kit": "^0.30.2"
  }
}
