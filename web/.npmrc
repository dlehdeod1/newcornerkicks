# @cloudflare/next-on-pages의 peer 범위가 낡아서 최신 wrangler/vercel과 충돌한다.
# (next-on-pages 1.13.16 → workers-types ^4 요구, wrangler 4.113.0 → workers-types ^5 요구)
# 버전을 고정하는 대신 peer 해석을 완화한다. 이게 없으면 Pages 빌드가 ERESOLVE로 실패함.
legacy-peer-deps=true
